name: Varsle med Gmail

on:
  schedule:
    - cron: '*/5 * * * *' # Kjører hvert 5. minutt
  workflow_dispatch: # Test-knapp

jobs:
  sjekk_og_varsle:
    runs-on: ubuntu-latest
    steps:
      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install Deps
        run: npm install @supabase/supabase-js nodemailer

      - name: Kjor Script
        env:
          SUPA_URL: ${{ secrets.SUPABASE_URL }}
          SUPA_KEY: ${{ secrets.SUPABASE_KEY }}
          GMAIL_USER: ${{ secrets.GMAIL_USER }}
          GMAIL_PASS: ${{ secrets.GMAIL_PASS }}
          APP_LINK: ${{ secrets.APP_URL }}
        run: |
          node -e '
          const { createClient } = require("@supabase/supabase-js");
          const nodemailer = require("nodemailer");
          
          const supabase = createClient(process.env.SUPA_URL, process.env.SUPA_KEY);

          const transporter = nodemailer.createTransport({
            service: "gmail",
            auth: {
              user: process.env.GMAIL_USER,
              pass: process.env.GMAIL_PASS,
            },
          });

          // Felles stil for e-post
          const mailHeader = `
            <!DOCTYPE html>
            <html>
            <body style="margin: 0; padding: 0; background-color: #f3f4f6; font-family: sans-serif;">
              <div style="max-width: 600px; margin: 20px auto; background: white; border-radius: 10px; overflow: hidden; box-shadow: 0 4px 6px rgba(0,0,0,0.1);">
                <div style="background-color: #1e293b; padding: 20px; text-align: center;">
                    <h1 style="color: white; margin: 0; font-size: 24px;">BookBil Jæren</h1>
                </div>
                <div style="padding: 30px;">
          `;
          
          const mailFooter = `
                </div>
                <div style="background-color: #f8fafc; padding: 15px; text-align: center; color: #64748b; font-size: 12px;">
                  <p>Dette er en automatisk melding fra bookingsystemet.</p>
                </div>
              </div>
            </body>
            </html>
          `;

          // Funksjon 1: Varsle bruker om kommende booking (Uendret logikk)
          async function checkBookings() {
            const now = new Date();
            const in30mins = new Date(now.getTime() + 30 * 60000);
            
            // Hent bookinger som starter snart og som tilhører brukere med varsling PÅ
            const { data: bookings } = await supabase
              .from("bookinger")
              .select("*, brukere!inner(varsel, epost)") // Inner join for å kun få de med varsel=true
              .gt("start_tid", now.toISOString())
              .lt("start_tid", in30mins.toISOString())
              .eq("varslet", false); // Eget felt i bookinger-tabellen for å unngå dobbelvarsling

            if (!bookings || bookings.length === 0) return;

            for (const b of bookings) {
               // Sjekk at brukeren faktisk har skrudd på varsel og har epost
               if (b.brukere && b.brukere.varsel && b.brukere.epost) {
                  const startTime = new Date(b.start_tid).toLocaleTimeString("no-NO", {hour:"2-digit", minute:"2-digit"});
                  
                  const htmlContent = `
                    <h2 style="color: #1e293b; margin-top: 0;">Din reservasjon starter snart</h2>
                    <p>Hei! Dette er en påminnelse om at du har reservert bil:</p>
                    <div style="background-color: #eff6ff; border-left: 4px solid #3b82f6; padding: 15px; margin: 20px 0;">
                        <p style="margin: 0; font-size: 18px; font-weight: bold; color: #1e293b;">${b.bil}</p>
                        <p style="margin: 5px 0 0; color: #475569;">Kl. ${startTime}</p>
                    </div>
                    <div style="text-align: center; margin-top: 30px;">
                        <a href="${process.env.APP_LINK}" style="background-color: #2563eb; color: white; padding: 12px 24px; text-decoration: none; border-radius: 6px; font-weight: bold;">ÅPNE APPEN</a>
                    </div>
                  `;

                  try {
                      await transporter.sendMail({
                        from: `"BookBil Jæren" <${process.env.GMAIL_USER}>`,
                        to: b.brukere.epost,
                        subject: `Påminnelse: ${b.bil} kl. ${startTime}`,
                        html: mailHeader + htmlContent + mailFooter
                      });
                      console.log(`Varsel sendt til ${b.brukere.epost}`);
                  } catch (e) { console.error("Feil ved sending:", e); }
               }
               // Merk som varslet uansett utfall for å unngå spam-loop
               await supabase.from("bookinger").update({ varslet: true }).eq("id", b.id);
            }
          }

          // Funksjon 2: SAMLE-LOGIKK for Meldinger til Bilansvarlig
          async function checkMessages() {
            // Hent alle usendte meldinger
            const { data: messages, error } = await supabase
              .from("meldinger")
              .select("*")
              .eq("sendt", false)
              .order("created_at", { ascending: true });

            if (error) { console.error("Feil ved henting:", error); return; }
            if (!messages || messages.length === 0) return; // Ingen nye meldinger

            // Hent liste over bilansvarlige
            const { data: config } = await supabase.from("konfigurasjon").select("verdi").eq("nokkel", "bilansvarlig_epost").maybeSingle();
            const adminEmails = config && config.verdi ? config.verdi.split(",").map(e => e.trim()).filter(e => e) : [];

            if (adminEmails.length === 0) {
               console.log("Ingen bilansvarlige konfigurert. Markerer meldinger som sendt for å tømme kø.");
               // Mark as sent to prevent infinite loop
               const ids = messages.map(m => m.id);
               await supabase.from("meldinger").update({ sendt: true }).in("id", ids);
               return;
            }

            // Bygg HTML-liste over hendelser
            let listHtml = "";
            messages.forEach(msg => {
                const timeStr = new Date(msg.created_at).toLocaleTimeString("no-NO", {
                  day: "2-digit", month: "2-digit", hour: "2-digit", minute: "2-digit"
                });
                
                // Fargekode basert på type melding
                let icon = "📝";
                let color = "#3b82f6"; // Blå default
                if(msg.type.includes("Skade") || msg.type.includes("Feil")) { icon = "⚠️"; color = "#ef4444"; }
                else if(msg.type.includes("Verksted")) { icon = "🔧"; color = "#a855f7"; }
                else if(msg.type.includes("Statusendring")) { icon = "🔄"; color = "#f59e0b"; }

                listHtml += `
                <div style="border-bottom: 1px solid #e2e8f0; padding: 15px 0;">
                    <div style="display: flex; justify-content: space-between; margin-bottom: 5px;">
                        <strong style="color: #0f172a; font-size: 16px;">${icon} ${msg.bil || "Ukjent bil"}</strong>
                        <span style="color: #64748b; font-size: 12px;">${timeStr}</span>
                    </div>
                    <div style="color: ${color}; font-weight: bold; font-size: 12px; text-transform: uppercase; margin-bottom: 5px;">${msg.type}</div>
                    <div style="color: #334155; font-size: 14px; background: #f8fafc; padding: 10px; border-radius: 6px; border: 1px solid #e2e8f0;">
                        ${msg.innhold}
                    </div>
                    <div style="color: #94a3b8; font-size: 11px; margin-top: 5px;">Registrert av: ${msg.bruker || "Ukjent"}</div>
                </div>
                `;
            });

            const digestHtml = `
                <h2 style="color: #1e293b; margin-top: 0;">Oppdatering fra Billageret</h2>
                <p>Det er registrert <strong>${messages.length}</strong> nye hendelser siden sist:</p>
                <div style="margin-top: 20px;">
                    ${listHtml}
                </div>
                <div style="text-align: center; margin-top: 30px;">
                    <a href="${process.env.APP_LINK}" style="background-color: #2563eb; color: white; padding: 12px 24px; text-decoration: none; border-radius: 6px; font-weight: bold;">GÅ TIL BILLAGER</a>
                </div>
            `;

            // Send én e-post til hver admin
            for (const email of adminEmails) {
                try {
                    await transporter.sendMail({
                      from: `"BookBil System" <${process.env.GMAIL_USER}>`,
                      to: email,
                      subject: `Billager: ${messages.length} nye hendelser`,
                      html: mailHeader + digestHtml + mailFooter
                    });
                    console.log(`Samle-varsel sendt til ${email}`);
                } catch (err) { console.error(`Feil ved sending til ${email}:`, err); }
            }

            // Marker alle som sendt i en batch
            const ids = messages.map(m => m.id);
            const { error: updateError } = await supabase.from("meldinger").update({ sendt: true }).in("id", ids);
            if(updateError) console.error("Feil ved oppdatering av status:", updateError);
            else console.log(`Markerte ${ids.length} meldinger som sendt.`);
          }

          async function run() {
            await checkBookings();
            await checkMessages();
          }
          
          run();
          '
